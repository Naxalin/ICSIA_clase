Pista: ¿Qué variable externa al efecto estás usando dentro de él? Esa variable debería ir en la matriz de dependencias.
La variable que estoy usando dentro del efecto es postId, así que esa es la que debo poner en la matriz de dependencias para que el efecto se ejecute cada vez que cambie.